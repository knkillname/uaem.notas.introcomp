# Práctica 8: Misión Rescate - Análisis de Datos Científicos 🚀🪐

**Objetivo**: Integrar herramientas de cómputo científico (**NumPy**, **Pandas**, **Matplotlib**, **SciPy**) y aprender computación simbólica con **SymPy** para analizar el aterrizaje de una sonda espacial.

---

## Historia 📜

La sonda espacial *"Explorer 1"* ha entrado en la atmósfera de un planeta desconocido. Durante el descenso, sus sensores registraron la altitud en diferentes momentos, pero la transmisión fue inestable debido a la ionización atmosférica.

Tenemos un archivo de datos "sucio" y necesitamos tu ayuda para:

1. Entender la física del movimiento usando matemáticas simbólicas (**SymPy**).
2. Limpiar y procesar los datos de telemetría (**Pandas**).
3. Calcular la gravedad del planeta basándonos en los datos (**SciPy**).
4. Visualizar la trayectoria del descenso (**Matplotlib**).

---

## Parte 1: Matemáticas Simbólicas con SymPy 🧮

Antes de tocar los números, usemos Python para hacer el álgebra por nosotros. **SymPy** es una biblioteca para matemática simbólica (como hacer álgebra en papel, pero en la computadora).

1. Instala SymPy si no lo tienes: `!pip install sympy`
2. Importa la librería:

    ```python
    import sympy as sp
    sp.init_printing() # Para que las fórmulas se vean bonitas
    ```

3. **Ejercicio**:
    * Define las variables simbólicas: $t$ (tiempo), $y_0$ (altura inicial), $v_0$ (velocidad inicial), $g$ (gravedad).
        * *Pista*: `t, y0, v0, g = sp.symbols('t y0 v0 g')`
    * Define la ecuación de movimiento de caída libre:
        $$ y(t) = y_0 + v_0 t - \frac{1}{2} g t^2 $$
    * Usa SymPy para calcular la **velocidad** $v(t)$ (la derivada de la posición respecto al tiempo).
        * *Pista*: `v = sp.diff(y, t)`
    * Usa SymPy para calcular la **aceleración** $a(t)$ (la derivada de la velocidad).
    * Resuelve simbólicamente la ecuación $y(t) = 0$ para encontrar el tiempo de impacto.
        * *Pista*: `sp.solve(y, t)`

---

## Parte 2: Los Datos de la Caja Negra (Pandas) 🐼

Los ingenieros han recuperado el siguiente log de datos de la "Caja Negra" de la nave. Copia y pega este código para generar tu DataFrame inicial:

```python
import pandas as pd
import io

csv_data = """Tiempo_s,Altitud_m,Estado_Sensor
0.0,1000.0,OK
1.0,985.5,OK
2.0,NaN,ERROR_01
3.0,925.1,OK
4.0,880.2,OK
5.0,820.5,OK
6.0,750.0,WARN_LOW_BAT
7.0,665.2,OK
8.0,570.8,OK
9.0,460.1,OK
10.0,340.5,OK
11.0,205.0,OK
12.0,55.2,OK
13.0,-100.0,CRASH
"""

df = pd.read_csv(io.StringIO(csv_data))
```

**Ejercicio**:

1. Muestra las primeras filas del DataFrame.
2. **Limpieza**:
    * Elimina las filas donde `Altitud_m` sea `NaN` (datos perdidos).
    * Elimina las filas donde la `Altitud_m` sea menor a 0 (errores de medición post-impacto).
3. Reinicia el índice del DataFrame si es necesario.

---

## Parte 3: Recuperando la Verdad (SciPy) 🔬

Ahora que tenemos datos limpios, vamos a descubrir en qué planeta estamos. Sabemos que la ecuación de movimiento es:
$$ h(t) = h_0 + v_0 t - 0.5 g t^2 $$

Usaremos `curve_fit` de SciPy para encontrar los valores de $h_0$, $v_0$ y $g$ que mejor se ajustan a nuestros datos.

**Ejercicio**:

1. Define una función en Python `modelo_caida(t, h0, v0, g)` que retorne la altura calculada.
2. Usa `scipy.optimize.curve_fit` pasando tus datos de tiempo y altitud limpios.
3. Imprime los valores óptimos encontrados.
4. **La Gran Pregunta**: Basado en el valor de $g$ obtenido, ¿en qué cuerpo celeste aterrizamos?
    * Tierra ($g \approx 9.8 m/s^2$)
    * Marte ($g \approx 3.7 m/s^2$)
    * Luna ($g \approx 1.6 m/s^2$)
    * Júpiter ($g \approx 24.8 m/s^2$)

---

## Parte 4: Visualización del Descenso (Matplotlib) 📊

Una imagen vale más que mil números.

**Ejercicio**:

1. Crea un gráfico que muestre:
    * Los puntos de datos reales (del DataFrame limpio) como puntos dispersos (`scatter`).
    * La curva de ajuste suave (usando los parámetros encontrados por SciPy) como una línea continua.
2. Añade etiquetas a los ejes ("Tiempo [s]", "Altitud [m]").
3. Añade una leyenda y un título ("Análisis de Descenso Explorer 1").
4. (Opcional) Marca en el gráfico el punto estimado de impacto (donde la altura llega a 0).

---

## Entregable 📝

Sube un cuaderno de Jupyter llamado `Practica8_TuNombre.ipynb` con todo el código y las respuestas a las preguntas planteadas. ¡Buena suerte en la misión!
